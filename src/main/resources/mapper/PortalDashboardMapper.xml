<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="aftnos.aftourismserver.admin.mapper.PortalDashboardMapper">

    <select id="sumNewsViewCount" resultType="long">
        SELECT IFNULL(SUM(view_count), 0)
        FROM t_news
        WHERE is_deleted = 0
    </select>

    <select id="sumNoticeViewCount" resultType="long">
        SELECT IFNULL(SUM(view_count), 0)
        FROM t_notice
        WHERE is_deleted = 0
    </select>

    <select id="sumScenicViewCount" resultType="long">
        SELECT IFNULL(SUM(view_count), 0)
        FROM t_scenic_spot
        WHERE is_deleted = 0
    </select>

    <select id="sumVenueViewCount" resultType="long">
        SELECT IFNULL(SUM(view_count), 0)
        FROM t_venue
        WHERE is_deleted = 0
    </select>

    <select id="sumActivityViewCount" resultType="long">
        SELECT IFNULL(SUM(view_count), 0)
        FROM t_activity
        WHERE is_deleted = 0
    </select>

    <select id="listLatestNews" resultType="aftnos.aftourismserver.admin.vo.ContentBriefVO">
        SELECT
            id,
            title,
            publish_time AS publishTime,
            view_count   AS viewCount
        FROM t_news
        WHERE is_deleted = 0
          AND status = 1
        ORDER BY publish_time DESC
        LIMIT #{limit}
    </select>

    <select id="listLatestNotices" resultType="aftnos.aftourismserver.admin.vo.ContentBriefVO">
        SELECT
            id,
            title,
            publish_time AS publishTime,
            view_count   AS viewCount
        FROM t_notice
        WHERE is_deleted = 0
          AND status = 1
        ORDER BY publish_time DESC
        LIMIT #{limit}
    </select>
</mapper>
