<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aftourism.portal.mapper.UserFavoriteMapper">

    <select id="findOne" resultType="com.aftourism.portal.pojo.UserFavorite">
        SELECT *
        FROM t_user_favorite
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.aftourism.portal.pojo.UserFavorite" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user_favorite(
            user_id,
            target_type,
            target_id,
            is_deleted
        ) VALUES (
            #{userId},
            #{targetType},
            #{targetId},
            0
        )
    </insert>

    <update id="delete" parameterType="long">
        UPDATE t_user_favorite
        SET is_deleted = 1
        WHERE id = #{id}
    </update>
</mapper>
